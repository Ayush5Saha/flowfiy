// Website-import: scrape a user's site and draft their business profile.
// Runs on the platform-funded Gemini key (GEMINI_API_KEY), same as the lead pipeline.
export const FEATURES: { websiteImport: boolean } = { websiteImport: true };
